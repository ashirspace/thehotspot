import { MailCheck, MessageSquareReply } from "lucide-react";
import { NewCampaignDialog } from "@/components/new-campaign-dialog";
import { StatusBadge } from "@/components/status-badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { getCampaigns } from "@/lib/data";
import { getViewer } from "@/lib/viewer";

export default async function CampaignsPage() { const viewer = (await getViewer())!; const campaigns = await getCampaigns(viewer.id); return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[.16em] text-primary">Outreach</p><h1 className="text-2xl font-semibold tracking-tight sm:text-3xl">Campaigns</h1><p className="mt-2 text-sm text-muted-foreground">Group outreach without turning your workflow into a full CRM.</p></div><NewCampaignDialog /></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{campaigns.map((campaign) => <Card key={campaign.id}><CardHeader><div className="flex items-start justify-between gap-3"><CardTitle>{campaign.name}</CardTitle><StatusBadge status={campaign.status} /></div><p className="pt-1 text-xs text-muted-foreground">Created {new Date(campaign.created_at).toLocaleDateString("en", { month: "short", day: "numeric" })}</p></CardHeader><CardContent><div className="grid grid-cols-2 gap-3 border-t pt-4"><div className="rounded-lg bg-muted p-3"><MailCheck className="mb-2 size-4 text-muted-foreground" /><p className="text-xl font-semibold">{campaign.sent_count}</p><p className="text-xs text-muted-foreground">Sent</p></div><div className="rounded-lg bg-muted p-3"><MessageSquareReply className="mb-2 size-4 text-muted-foreground" /><p className="text-xl font-semibold">{campaign.reply_count}</p><p className="text-xs text-muted-foreground">Replies</p></div></div></CardContent></Card>)}</div></div>; }
